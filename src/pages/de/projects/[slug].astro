---
import Page from '../../projects/[slug].astro';
export { getStaticPaths } from '../../projects/[slug].astro';

export const prerender = import.meta.env.ADMIN_BUILD !== true;
---
<Page {...Astro.props} />
