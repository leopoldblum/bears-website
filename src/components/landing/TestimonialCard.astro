---
// Testimonial Card Component
import Img from "@mdx/Img.astro";
import type { ImageMetadata } from "astro";

interface Props {
  quote: string;
  name: string;
  role: string;
  image: ImageMetadata;
}

const { quote, name, role, image } = Astro.props;
---

<div
  class="w-full bg-bears-bg-light rounded-2xl border border-white/10 shadow-[0_4px_12px_rgba(0,0,0,0.4)] py-10 sm:py-10 lg:py-12 px-6 sm:px-8 lg:px-10"
>
  <div
    class="flex flex-col lg:flex-row gap-8 lg:gap-16 xl:gap-20 lg:items-center"
  >
    <!-- Left: Image (1/3 width on desktop) -->
    <div
      class="testimonial-image-wrapper shrink-0 mx-auto lg:mx-0 w-48 sm:w-48 lg:w-1/3 aspect-square overflow-hidden rounded-xl shadow-[0_8px_24px_rgba(0,0,0,0.5)] bg-white"
    >
      <Img
        src={image}
        alt={name}
        fill={true}
        loading="lazy"
        sizes="(min-width: 1024px) 16vw, 192px"
        enableClickToEnlarge={false}
      />
    </div>

<style>
  .testimonial-image-wrapper :global(img) {
    width: 100% !important;
    height: 100% !important;
    object-fit: cover !important;
    object-position: center !important;
  }
</style>

    <!-- Right: Content (2/3 width on desktop) -->
    <div
      class="flex-1 lg:w-2/3 flex flex-col justify-center text-center lg:text-left pr-0 lg:pr-8"
    >
      <!-- Name -->
      <h3
        class="text-xl sm:text-2xl lg:text-3xl font-extrabold text-bears-text-onLight mb-2"
      >
        {name}
      </h3>

      <!-- Role -->
      <p
        class="text-sm sm:text-base lg:text-lg text-bears-accent-muted mb-4 lg:mb-6"
      >
        {role}
      </p>

      <!-- Quote -->
      <p
        class="text-base sm:text-lg lg:text-xl text-bears-text-onLight leading-relaxed italic"
      >
        "{quote}"
      </p>
    </div>
  </div>
</div>
