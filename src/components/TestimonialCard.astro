---
// Testimonial Card Component
import { Image } from 'astro:assets';
import type { ImageMetadata } from 'astro';

interface Props {
  quote: string;
  name: string;
  role: string;
  image: ImageMetadata;
}

const { quote, name, role, image } = Astro.props;
---

<div class="w-full bg-bears-bg-light rounded-2xl p-6 sm:p-8 lg:p-10">
  <div class="flex flex-col lg:flex-row gap-8 lg:gap-10 xl:gap-12 min-h-100 lg:items-center">
    <!-- Left: Image (1/3 width on desktop) -->
    <div class="shrink-0 mx-auto lg:mx-0 lg:w-1/3 flex items-center justify-center">
      <Image
        src={image}
        alt={name}
        class="w-40 sm:w-48 lg:w-full aspect-square rounded-lg object-cover"
        loading="lazy"
        format="webp"
      />
    </div>

    <!-- Right: Content (2/3 width on desktop) -->
    <div class="flex-1 lg:w-2/3 flex flex-col justify-center text-center lg:text-left">
      <!-- Name -->
      <h3 class="text-xl sm:text-2xl lg:text-3xl font-extrabold text-bears-text-onLight mb-2">
        {name}
      </h3>

      <!-- Course of Studies / Role -->
      <p class="text-sm sm:text-base lg:text-lg text-bears-text-onLight opacity-75 mb-4 lg:mb-6">
        {role}
      </p>

      <!-- Quote -->
      <p class="text-base sm:text-lg lg:text-xl text-bears-text-onLight leading-relaxed italic">
        "{quote}"
      </p>
    </div>
  </div>
</div>
